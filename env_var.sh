#!/bin/bash

# export <var(mobile)=content(pixel mobile)> to add the environmental variable. 

echo "current using mobile is: $mobile"


# this env will clear once the session is closed, but if it needed for further purpose 
# add this export var="content" to .bashrc file before unset rc and save the file 
#
# after editing run 
# source .bashrc
#
# .bashrc will be in home dir. 
